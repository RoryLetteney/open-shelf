<!DOCTYPE html>
<html lang="en">
<%- include ../../layout/head.ejs %>
<body>
  <%- include ../../layout/header.ejs %>
  <main>
    <h2>Search Results</h2>
    <% results.forEach(book => { %>
      <section class="book">
        <%- include ../books/detail.ejs %>
        <form id="save-book" class="hide-me" action="/save" method="POST">
          <input name="author" value="<%= book.author %>">
          <input name="title" value="<%= book.title %>">
          <input name="image_url" value="<%= book.image_url %>">
          <input name="description" value="<%= book.description %>">
          <input name="isbn" value="<%= book.isbn %>">
          <input name="bookshelf" value="<%= book.bookshelf %>">
        </form>
        <input form="save-book" type="submit" value="Save This Book">
      </section>
    <% }); %>
  </main>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.0/jquery.min.js"></script>
</body>
</html>